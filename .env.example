# API Keys
# Store API keys in macOS Keychain:
#   security add-generic-password -U -a "$USER" -s com.rpzy.env.DEEPSEEK_API_KEY -w "..."
#   security add-generic-password -U -a "$USER" -s com.rpzy.env.GOOGLEAI_API_KEY -w "..."
# TypeScript Server Settings
TSS_MAX_MEMORY=4096
